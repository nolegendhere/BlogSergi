/* Add the new comment to the bottom of the comments list */
$('.usercomments').append("<%= escape_javascript(render @comment) %>");

/* Reset the comment form -->form has new_comment as a class and as a id, probably generated by form_for*/
$(".new_comment")[0].reset();
