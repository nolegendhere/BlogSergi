<%= form_for(current_user.subscription.active_relationships.find_by(post_id: @post.id),
             html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Unfollow", class: "btn" %>
<% end %>