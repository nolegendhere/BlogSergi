  <div id="follow_form">
  <% if !current_user.subscribed %>	
  	  <%= render 'follow' %>
  <% else %>
	  <% if current_user.subscription.following?(@post) %>
	    <%= render 'unfollow' %>
	  <% else %>
	    <%= render 'follow' %>
	  <% end %>
  <% end %>
  </div>
